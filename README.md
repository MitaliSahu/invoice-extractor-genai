# Automated Invoice Data Extractor

This project extracts structured invoice data from PDF files using Python, pdfplumber, and OpenAI.

## Features
- Extracts invoice text from PDF
- Cleans text
- Uses GPT-4o-mini to extract structured fields
- Saves output as JSON

## Installation

```bash
pip install -r requirements.txt